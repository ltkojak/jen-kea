# Jen development + CI tooling.
#
# Runtime dependencies come in via the -r include below — this file is
# only the extra tools CI and contributors need. Mostly unpinned; ruff
# is pinned exact (v5.6.0) so `ruff format --check .` in CI is a stable
# gate — its output shifts subtly between releases.

-r requirements.txt

pytest
bandit
pip-audit
ruff==0.16.6

# Browser end-to-end suite (tests/e2e/, v5.39.0/Q40) — excluded from the
# default `pytest`/`pytest tests/` run (tests/e2e/conftest.py
# importorskip()s playwright, so the rest of the suite never notices
# it's absent). Only needed to run tests/e2e/ explicitly, or in the CI
# `e2e` job, which also runs `playwright install --with-deps chromium`.
playwright
pytest-playwright
